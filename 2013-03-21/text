2013/03/21

Bytecode 2011

A unsolved
B unsovled
C 给若干个x>a,x<a,x=a,x！=a的式子，问选取一个x的值，使得符合条件的式子最多。
  可将x=a拆为x>=a&&x<=a，把x!=a拆为x<a&&x>a
  然后就是区间加一个值，然后查区间最大值了。
D 水题
E unsovled 
F 水题
G 给一棵树，初始为无色，每次给一个点染色，然后询问两个点之间路径的颜色是否相同，保证每个点不会被染多次。
    可以用并查集维护，fa[a]表示为a的祖先，且这段路径上的点颜色相同。每次查询如果find(a)=find(b)则表示两点间路径颜色相同，否则不同。
H 问N个长为L的随机01串形成trie树的期望长度。
	每次可将L分为两部分，一部分代表第一位为0，另一部分代表第一位为1。这样就变成了子问题，记忆化搜复杂度O(N^3)
I 给出所用1到9数字的个数，然后询问其中两个串a,b。问a,b在所有排列中的rank,以及两个串间所有串的和
	数位DP的思想，记录状态麻烦些。。。
J 貌似DP，unsolved 
K 线段树
