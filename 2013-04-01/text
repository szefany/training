2013/04/01
Latin America - South America - 2010/2011

A 询问树上两点间距离，lca
B 简单题
C 求平面上能用圆周覆盖点数最多的圆，HashMap/Sort
D 区间内每个数位出现次数，数位dp
E Unsolved
F 简单题
G 给定串，串A与B能连接当且仅当A是B的子串。求令连接串数最多。
    对所有串按长度增序排序并插入AC自动机后。按顺序让每个串在自动机里跑，dp[i]表示以单词i为结尾的最多串数。于是dp[i] = max{dp[j] + 1}, j为路径上经过的点所映射到的之前已经计算过的长度比他小的串。注意j也可以是当前节点的fail，两边一起更新。
H dp
I 给定数轴上一些点，然后给若干组查询，每次判断从a点能否到b点，移动的要求是每次从a找到一个和初始给定点轴对称的一个点。比如初始给了x，那么下次跳到2*x-a
	做法就是如果符合条件，那么b=a+2*(xi+...+xj)(共m个）-2*(xk+...+xl)(共m)个，或者b=-a+2*(xi+...+xj)(共m+1个）-2*(xk+...+xl)(共m)个
	我们先看第一种
	如果我们用ai表示x(i+1)-x(i)，那么我们可以得到任意的xi-xj，通过若干个ai相加。
	原题就被化为能否通过ai构造出b-a
	求所有ai的最大公约数然后判能否被b-a整除即可。
	另一种情况同上，只不过需要枚举下xi
J 简单题
K 给定N个点的环以及若干条件(a, b)，必须满足(a, b)相邻，问是否可行。判定度数小于3或环的长度等于N - 1。
